micronaut:
  application:
    name: shops
    instance:
      id: ${random.shortuuid}
  config-client:
    enabled: true
db:
  name: 'shops'
  collection: 'shops'
netty:
  default:
    allocator:
      max-order: 3
endpoints:
  health:
    enabled: true
    sensitive: false